function multiplyBy2(input) {
    return input * 2
}

const multiplyBy2 = (input) => {return input * 2}

// skip curly braces and return keyword if you are only return 1 output.
const multiplyBy2 = (input) => input * 2

// if there is only 1 parameter in a function, then there is no need to add parenthesis
const multiplyBy2 = input=> input * 2

const output = multiplyBy2(3)

